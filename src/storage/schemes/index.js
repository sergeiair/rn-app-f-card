import { fishScheme } from './fish.scheme';
import { rulesScheme } from './rules.scheme';
import { placesScheme } from './places.scheme';

export const dbSchemes = {
  fishes: [fishScheme],
	rules: [rulesScheme],
	places: [placesScheme],
};

